import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';

const LoginTest = () => {
    return (
        <div style={{ padding: '4rem', textAlign: 'center' }}>
            <h1>LOGIN PAGE TEST</h1>
            <p>If you see this, Login is rendering.</p>
        </div>
    );
};

export default LoginTest;
